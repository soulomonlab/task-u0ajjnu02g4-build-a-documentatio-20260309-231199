Title: ML Impact Assessment — update_docs_from_pr

Situation
- Backend PR 'update_docs_from_pr' includes API surface changes, new env vars, and observability requirements.

Complication
- Several API changes are near-breaking for ML integration: pagination semantics changed, 409 conflict behaviors, token rotation endpoints/flows added, and new observability env vars introduced with unclear names/format. QA gaps were uncovered (token rotation, pagination validation) that affect model inference and monitoring.

Resolution — Actionable ML-focused items (for engineering + QA)

1) ML client & inference service changes (implementation required)
   - Update client-side request shapes to match new API (fields X, Y renamed → new names). Verify contract with backend swagger.
   - Implement 409 Conflict retry: exponential backoff with jitter; max 5 retries; circuit-breaker fallback to async queue after 3 failed attempts.
   - Token rotation: implement refresh-token flow that attempts a silent refresh on 401, with a single retry of the original request after refresh; log and escalate if refresh fails.
   - Pagination validation: validate server-provided pagination cursors; fail-safe to page-by-offset (if provided) or request smaller page sizes if cursor invalid.

2) Observability & env vars (must be coordinated with DevOps / Backend)
   - Required env vars (suggested names): ML_METRICS_ENDPOINT, ML_TRACE_SAMPLING_RATE (0.01 default), MODEL_VERSION, TOKEN_ROTATION_URL.
   - Emit these ML-specific metrics: inference_latency_ms (p50/p95/p99), prediction_success_rate, model_input_schema_hash, model_version.
   - Tag logs/metrics with request_id and api_version to correlate backend traces.

3) Tests & acceptance criteria (to hand off to QA)
   - Integration smoke tests: verify token rotation flow, pagination across boundary conditions (empty, last page, invalid cursor), 409 retry behavior.
   - Performance targets: inference p95 < 50ms (real-time path), end-to-end API+model p95 < 150ms for common requests.
   - Monitoring: drift detector baseline saved; retrain trigger if accuracy drops >5%.

4) Data checks (coordination with Data)
   - Provide 3 months of representative samples for pagination edge cases and token-scoped datasets.
   - Sanity-check feature distributions after API changes (ensure no broken or missing fields).

5) Rollout & mitigation plan
   - Start with shadow mode for new API for 48 hours, compare predictions and metrics vs current API.
   - Canary: 5% → 20% → 100% traffic if no regression in metrics after 24h.

Acceptance checklist (ML owner responsibility)
- ML client updated and tested locally against mocked new API
- Integration smoke tests passing in CI for token rotation, pagination, 409 handling
- Required env vars documented and set in staging
- Observability metrics emitted and visible in metrics dashboard
- Drift baseline saved; retrain job configured to run if threshold exceeded

Dependencies / Who to coordinate with
- Backend (Marcus): confirm API contract, swagger, and 409 semantics; provide token rotation endpoint details
- DevOps (Noah): add env vars to staging/prod and confirm metrics ingestion pipeline
- Data (Samantha): provide sample datasets for testing

Estimated effort (engineering)
- ML client + tests: 2 engineer-days
- Observability + metric plumbing: 1 engineer-day
- Integration QA + coordination: 1 engineer-day

Files/changes ML will produce (planned)
- output/code/ml/update_client_adapter.py — client updates + retry logic
- output/code/ml/tests/integration/test_token_rotation.py
- output/code/ml/monitoring/emit_metrics.py
- output/code/ml/monitoring/drift_detector.py

Notes / Open questions for Backend
- Exact field renames & swagger link (required)
- Canonical env var names and where metrics should be pushed (Prometheus/Datadog endpoint)
- 409 semantics: should client retry immediately or after backoff? confirm desired SLA/throughput trade-offs

---
Generated by: Lisa (ML/AI Engineer)
Date: 2026-03-09
