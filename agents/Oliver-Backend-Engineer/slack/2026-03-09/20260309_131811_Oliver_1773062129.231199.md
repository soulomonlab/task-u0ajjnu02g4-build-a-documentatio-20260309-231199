# Agent Output

- project: `Main Product`
- agent: `Oliver`
- channel: `C0AJ0740LNB`
- thread_ts: `1773062129.231199`
- message_ts: `1773062291.681719`
- source: `slack`

---

## Database Design for API changes: token-rotation, pagination metadata, and 409 semantics

### Tables
- external_api_responses
  - id BIGSERIAL PRIMARY KEY
  - source_service VARCHAR(100) NOT NULL     -- e.g., "docs", "anthropic-worker"
  - external_id VARCHAR(200) NULL            -- id provided by upstream (may collide)
  - payload JSONB NOT NULL                   -- full upstream payload
  - pagination_meta JSONB NULL               -- normalized pagination metadata extracted from payload
  - received_at TIMESTAMPTZ NOT NULL DEFAULT now()
  - processed_at TIMESTAMPTZ NULL
  - ingestion_status VARCHAR(50) NOT NULL DEFAULT 'pending' -- pending/processed/error/duplicate
  - dedupe_key VARCHAR(255) NULL            -- deterministic key used to dedupe on 409 semantics
  - token_used VARCHAR(255) NULL            -- which token value or token id used for the request (for rotation traceability)
  - upstream_status_code INT NULL           -- upstream HTTP status code (e.g., 200, 409)
  - error JSONB NULL

- external_api_tokens
  - id BIGSERIAL PRIMARY KEY
  - name VARCHAR(100) NOT NULL UNIQUE       -- environment pointing name, e.g., DOCS_API_TOKEN_PRIMARY
  - token_hash VARCHAR(128) NOT NULL        -- hashed token or identifier (avoid storing raw tokens)
  - provider_token_id VARCHAR(255) NULL     -- id returned by provider if any
  - rotated_at TIMESTAMPTZ NULL
  - expires_at TIMESTAMPTZ NULL
  - status VARCHAR(50) NOT NULL DEFAULT 'active' -- active/rotated/revoked
  - metadata JSONB NULL

- ingestion_conflicts (audit for 409 / duplicates)
  - id BIGSERIAL PRIMARY KEY
  - external_api_response_id BIGINT NOT NULL REFERENCES external_api_responses(id) ON DELETE CASCADE
  - conflict_key VARCHAR(255) NOT NULL
  - conflict_type VARCHAR(50) NOT NULL     -- e.g., "409-duplicate", "

...(truncated)